import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
    domains: ['localhost', 'market-assets.strapi.io'], // agrega aquí todos los hostnames que uses
  },
};

export default nextConfig;
